{
  "name": "fretwork",
  "version": "1.0.0",
  "description": "Guitar scales...",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "npm run build && electron ./dist/main.js --remote-debugging-port=9223",
    "start": "npm run build && electron ./dist/main.js"
  },
  "author": "Edgars Binans",
  "license": "MIT",
  "devDependencies": {
    "electron": "^11.1.0",
    "typescript": "^4.1.3"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/ebinans/fretwork.git"
  }
}
